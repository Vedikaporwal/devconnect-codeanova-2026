import { Search, SlidersHorizontal, UsersRound } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { DeveloperCard } from "@/components/developer-card";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import { useDiscovery } from "@/hooks/use-discovery";
import { useEffect, useState } from "react";

export default function Discover() { const [draft, setDraft] = useState({ search: "", skill: "", location: "" }); const [filters, setFilters] = useState(draft); useEffect(() => { const timer = window.setTimeout(() => setFilters(draft), 350); return () => window.clearTimeout(timer); }, [draft]); const query = useDiscovery(filters); const developers = query.data ?? []; return <AppShell><div className="mx-auto max-w-[1320px] px-5 py-8 sm:px-8 lg:px-12 lg:py-12"><header className="border-b border-border pb-8"><div className="mb-5 flex items-center gap-3 font-mono text-[10px] uppercase tracking-[.2em] text-primary"><UsersRound size={14} /> Developer index / discovery</div><h1 className="text-4xl font-semibold tracking-[-.065em] sm:text-6xl">Find the people behind <span className="text-primary">the work.</span></h1><p className="mt-4 max-w-2xl text-base leading-relaxed text-muted-foreground">Search for developers by what they build, notice, and want to talk about.</p></header><section className="my-8 grid gap-3 rounded-2xl border border-border bg-secondary/30 p-4 md:grid-cols-[1.5fr_1fr_1fr_auto]" aria-label="Developer filters"><label className="relative"><Search size={16} className="absolute left-3 top-3 text-primary" /><Input value={draft.search} onChange={(event) => setDraft({ ...draft, search: event.target.value })} placeholder="Search name, bio, or headline" className="h-11 pl-9" /></label><Input value={draft.skill} onChange={(event) => setDraft({ ...draft, skill: event.target.value })} placeholder="Skill, e.g. TypeScript" className="h-11" /><Input value={draft.location} onChange={(event) => setDraft({ ...draft, location: event.target.value })} placeholder="Location" className="h-11" /><Button variant="outline" onClick={() => setDraft({ search: "", skill: "", location: "" })} className="gap-2"><SlidersHorizontal size={15} /> Clear</Button></section>{query.isLoading && <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{[1,2,3].map((item) => <div key={item} className="soft-card h-80 animate-pulse rounded-2xl" />)}</div>}{!query.isLoading && !developers.length && <div className="soft-card rounded-2xl border-dashed p-12 text-center"><UsersRound className="mx-auto text-primary" /><h2 className="mt-5 text-2xl font-medium">No matching developers.</h2><p className="mt-2 text-sm text-muted-foreground">Try a broader signal or clear the filters.</p></div>}{developers.length > 0 && <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{developers.map((developer) => <DeveloperCard key={developer.id} developer={developer} />)}</div>}</div></AppShell>; }