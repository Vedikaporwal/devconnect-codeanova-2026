import { ArrowUpRight, Globe2, MapPin } from "lucide-react";
import { Link } from "wouter";
import type { PublicDeveloper } from "@workspace/shared";

const initials = (name: string) => name.split(/\s+/).slice(0, 2).map((part) => part[0]?.toUpperCase()).join("");
export function DeveloperCard({ developer }: { developer: PublicDeveloper }) { return <article className="soft-card group rounded-2xl p-5 transition-transform duration-300 hover:-translate-y-1 hover:border-primary/35"><div className="flex items-start justify-between"><div className="grid h-14 w-14 place-items-center overflow-hidden rounded-2xl border border-primary/30 bg-primary/10 font-mono text-primary">{developer.avatarUrl ? <img src={developer.avatarUrl} alt="" className="h-full w-full object-cover" /> : initials(developer.name)}</div><Link href={`/profile/${developer.id}`} aria-label={`View ${developer.name}'s profile`} className="grid h-9 w-9 place-items-center rounded-full border border-border text-muted-foreground hover:border-primary hover:text-primary"><ArrowUpRight size={16} /></Link></div><p className="mt-8 font-mono text-[10px] text-primary">@{developer.username}</p><h2 className="mt-1 text-2xl font-medium tracking-[-.04em]">{developer.name}</h2><p className="mt-1 min-h-10 text-sm text-muted-foreground">{developer.headline || "Developer profile"}</p><div className="mt-5 flex flex-wrap gap-3 text-xs text-muted-foreground">{developer.location && <span className="flex items-center gap-1.5"><MapPin size={13} />{developer.location}</span>}<span className="flex items-center gap-1.5"><Globe2 size={13} />Public profile</span></div><p className="mt-5 line-clamp-2 text-sm leading-relaxed text-muted-foreground">{developer.bio || "Sharing the work and context behind the build."}</p><div className="mt-6 flex flex-wrap gap-1.5">{developer.skills.slice(0, 5).map((skill) => <span key={skill} className="rounded-md border border-border bg-background/45 px-2 py-1 font-mono text-[10px] text-muted-foreground">{skill}</span>)}</div></article>; }