import { ArrowUpRight, Calendar } from "lucide-react";
import { Link } from "wouter";
import type { BlogPost } from "@workspace/shared";

export function BlogCard({ blog }: { blog: BlogPost }) { return <article className="soft-card group rounded-2xl p-5 transition-transform duration-300 hover:-translate-y-1 hover:border-primary/35"><div className="flex items-center justify-between font-mono text-[10px] uppercase tracking-[.14em] text-muted-foreground"><span className={blog.published ? "text-primary" : "text-orange-300"}>{blog.published ? "Published" : "Draft"}</span><span className="flex items-center gap-2"><Calendar size={12} /> {new Intl.DateTimeFormat("en", { month: "short", day: "numeric", year: "numeric" }).format(new Date(blog.createdAt))}</span></div><h2 className="mt-12 text-2xl font-medium tracking-[-.04em]">{blog.title}</h2><p className="mt-3 line-clamp-3 text-sm leading-relaxed text-muted-foreground">{blog.excerpt || blog.content.slice(0, 180)}</p><div className="mt-8 flex items-center justify-between border-t border-border pt-4"><span className="text-xs text-muted-foreground">By {blog.author.name}</span><Link href={`/blogs/${blog.id}`} className="flex items-center gap-2 text-xs font-semibold text-primary">Read post <ArrowUpRight size={14} /></Link></div></article>; }