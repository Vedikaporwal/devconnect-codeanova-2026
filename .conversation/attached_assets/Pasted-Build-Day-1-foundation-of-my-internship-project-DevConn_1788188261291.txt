Build Day 1 foundation of my internship project "DevConnect", a developer networking and portfolio platform.

IMPORTANT: Implement ONLY Day 1. Do not build authentication or any Day 2+ features.

TECH STACK:
- React 18+ + Vite + TypeScript
- Tailwind CSS
- React Router
- TanStack React Query
- Zustand
- Node.js + Express + TypeScript
- PostgreSQL + Prisma
- Monorepo with /client, /server, /shared

STRUCTURE:

/client
/server
/shared
/prisma
package.json
.env.example
.gitignore
README.md

CLIENT:
Create a React/Vite/TypeScript frontend with:
- React Router
- TanStack Query provider
- Zustand store foundation
- API service layer
- VITE_API_URL environment configuration
- reusable UI components

SERVER:
Create Express + TypeScript API with:

server/src/
  config/
  controllers/
  middleware/
  routes/
  services/
  utils/
  types/
  app.ts
  server.ts

Create:
GET /api/health

Response must be:

{
  "success": true,
  "data": {
    "status": "ok"
  },
  "message": "DevConnect API is running"
}

Add:
- centralized error handling
- CORS using CLIENT_URL
- environment configuration
- clean separation of routes/controllers/services

SHARED:
Create a reusable TypeScript:

ApiResponse<T> {
  success: boolean;
  data: T;
  message: string;
}

Make /shared usable by both client and server.

DATABASE:
Set up Prisma for PostgreSQL only.
Create the Prisma foundation but DO NOT create the Day 2 database models yet.

Do NOT use MongoDB, FastAPI, Python, Firebase or Supabase.

ENVIRONMENT:
Create .env.example containing:

DATABASE_URL=
JWT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLIENT_URL=
SERVER_URL=
VITE_API_URL=

Never expose server secrets to the frontend.
Add .env to .gitignore.

LANDING PAGE:
Create a premium, modern and responsive DevConnect landing page.

Design:
- dark developer/SaaS aesthetic
- background #071014
- surface #111C22
- text #F5F7F8
- muted text #8A9AA3
- accent #B7FF3C
- subtle cyan accents
- clean typography
- modern cards
- subtle borders
- smooth hover effects
- tasteful animations
- responsive mobile/tablet/desktop layout

Hero headline:

"The internet is full of code.
Find the people behind it."

Add:
- DevConnect logo
- short description
- "Join DevConnect" CTA
- "Explore Developers" CTA
- navigation
- feature preview cards
- How It Works section
- final CTA
- footer

Feature preview cards:
1. Developer Profiles
2. Project Showcase
3. Technical Blogs
4. Developer Network

These are UI previews only. Do not implement their functionality.

QUALITY:
Make the UI look like a real developer startup product, not a basic template.
Use reusable components and avoid unnecessary dependencies.

DO NOT IMPLEMENT:
- login/register
- JWT authentication
- GitHub OAuth
- user/profile CRUD
- projects CRUD
- blogs/Markdown
- developer search
- connections
- endorsements
- Socket.io
- notifications
- Cloudinary uploads
- dashboard
- deployment

Before finishing, verify:
1. frontend runs
2. backend runs
3. landing page renders
4. /api/health works
5. TypeScript has no obvious errors
6. monorepo structure is correct
7. no MongoDB/FastAPI/Python is used
8. .env is ignored

Also create a concise README with setup instructions and Day 1 status.

Do not make changes outside Day 1.